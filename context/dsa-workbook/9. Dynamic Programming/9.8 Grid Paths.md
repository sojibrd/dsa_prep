### 9.8 Grid Paths

**চিনবেন কীভাবে:** grid-এ উপরে-বাম থেকে নিচ-ডানে "কত path" বা "min cost path" — dp[i][j] = উপর + বাম

**Demo: Unique Paths** — [LC 62](https://leetcode.com/problems/unique-paths/) _(Medium — Google/Meta High)_

**Statement (Demo):** `m × n` grid-এর top-left cell থেকে bottom-right cell-এ যাওয়ার মোট কতটি অনন্য path আছে বলুন (একবার কেবল নিচে বা ডানে যাওয়া যাবে)।
উদাহরণ: `m=3, n=7` → `28` | ⚡ `m,n ≤ 100`

**Approach:** প্রতিটা ঘরে আসা যায় উপর বা বাম থেকে — `dp[i][j] = dp[i-1][j] + dp[i][j-1]`। এক row-ই যথেষ্ট: in-place রোল করুন।

```js
function uniquePaths(m, n) {
  const dp = new Array(n).fill(1); // প্রথম row
  for (let i = 1; i < m; i++) for (let j = 1; j < n; j++) dp[j] += dp[j - 1]; // উপর (পুরনো dp[j]) + বাম
  return dp[n - 1];
}
```

**Complexity:** Time O(m·n), Space O(n)

**প্রবলেম লিস্ট:**

- [ ] **Minimum Path Sum** — [LC 64](https://leetcode.com/problems/minimum-path-sum/) — ⚪ Bonus
      → Statement: numbers সম্বলিত `m × n` grid দেওয়া — top-left থেকে bottom-right-এ পৌঁছাতে সর্বনিম্ন কত যোগফলের path তৈরি করা যাবে বলুন।
      উদাহরণ: `grid=[[1,3,1],[1,5,1],[4,2,1]]` → `7` (1->3->1->1->1) | ⚡ `m,n ≤ 200`
      → আমার সমাধান:
      → যে সমস্যা হয়েছিল:
- [ ] **Knight Probability in Chessboard** — [LC 688](https://leetcode.com/problems/knight-probability-in-chessboard/) — ⚪ Bonus _(probability DP — state: (ধাপ, ঘর), সম্ভাবনা যোগ হয়)_
      → Statement: `n × n` chessboard-এ একটি ঘোড়া (knight) `(r, c)` ঘরে বসানো আছে। ঘোড়াটি `k`টি লফ দেওয়ার পরও বোর্ডের ভেতরেই থাকবে তার সম্ভাবনা (probability) কত বলুন।
      উদাহরণ: `n=3, k=2, r=0, c=0` → `0.0625` | ⚡ `n ≤ 25`, `k ≤ 100`
      → আমার সমাধান:
      → যে সমস্যা হয়েছিল:
